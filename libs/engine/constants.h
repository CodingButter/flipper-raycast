// Engine-wide math constants. No subsystem-specific values here — those
// live in their respective config / world-api headers.
#pragma once

constexpr float PI      = 3.14159265f;
constexpr float HALF_PI = PI * 0.5f;
constexpr float EPS     = 0.0001f;
